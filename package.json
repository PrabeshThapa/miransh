{
  "name": "miransh-laravel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "php artisan serve --host=0.0.0.0 --port=3000",
    "start": "php artisan serve --host=0.0.0.0 --port=3000",
    "lint": "php artisan about",
    "build": "php artisan optimize:clear"
  }
}
